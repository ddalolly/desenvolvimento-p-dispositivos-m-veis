/* teste utilizando o laço de repetição for */

let i=5000 //é um escopo global, aparece no código inteiro
for (let i= 1000; i< 5000; i+=100) {
    console.log(i);
}
console.log(`UFA, até que enfim estou ganhando ${i} reais!!!`);